db.getCollection('eikonTwo').aggregate([  { $group: {    _id: { EikonTicker: "$EikonTicker" },   // replace `name` here twice    dups: { $addToSet: "$_id" },    count: { $sum: 1 }  } },  { $match: {    count: { $gt: 1 }  } }]).forEach(function(doc) {   doc.dups.shift();   db.getCollection('eikonTwo').remove({       _id: {$in: doc.dups}   });});